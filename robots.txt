# clients.brand-os.ru — хаб клиентских материалов BrandOS.
# Все страницы закрыты паролём (StatiCrypt). Обход запрещён для всего хоста.
#
# Заведено 12.09.2026 решением владельца. Причина: StatiCrypt заменяет документ целиком,
# и <meta name="robots" content="noindex"> исходной страницы уезжает внутрь шифротекста —
# на опубликованной оболочке его нет ни на одной странице (замер: 0 из 242).
# Запрет ставится одним файлом в корне, потому что так не меняется ни одна существующая страница.
User-agent: *
Disallow: /
